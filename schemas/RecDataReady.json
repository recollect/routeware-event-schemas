{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "com.routeware.eventbus.RecDataReady",
    "type": "object",
    "default": {},
    "title": "RecDataReady Schema",
    "required": [
    ],
    "properties": {
        "input_file_url": {
            "type": "string",
            "default": "",
            "title": "URL to the input file to be processed",
            "examples": [
                "https://example.com/path/to/some/file"
            ]
        },
        "connector_name": {
            "type": "string",
            "default": "default",
            "title": "Name of the connector to be used, if necessary.",
            "examples": [
                "default"
            ]
        }
    },
    "examples": [{
        "input_file_url": "https://example.com/path/to/some/file"
    }]
}
